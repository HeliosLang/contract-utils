spending match_string

import { compare } from "./utils.hl"

enum Datum {
	One {
		message: String
	}

	Two {
		code: Int
	}
}

func main(datum: Datum, redeemer: String, _) -> Bool {
	compare(datum.switch{d: One => d.message, d: Two => d.code.show()}, redeemer)
}
